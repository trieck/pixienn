[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pixienn"
version = "0.1.0"
description = "Python interface to PixieNN"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
